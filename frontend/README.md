# Kavya Margin — Frontend

Vite + React demo frontend.

Run:

```bash
cd frontend
npm install
npm run dev
```

The dev server runs at `http://localhost:5173` and talks to the backend at `http://localhost:4000`.

front end working  is going on...
